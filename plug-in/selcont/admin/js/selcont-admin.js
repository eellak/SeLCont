(function( $ ) {
	'use strict';

    $(function() {

        if( 0 < $('#slide_image_meta_box').length ) {
            $('form').attr('enctype', 'multipart/form-data');
        } // end if

    });

})( jQuery );

